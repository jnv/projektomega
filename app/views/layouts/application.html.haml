!!! 5
-# http://paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither
- html_tag :class => "no-js", :lang => "cs" do
  = render "head"

  %body{ :class => "#{ controller.controller_name }" }
    #container
      = render "header"

      #main{ :role => "main" }
        #page_title
          %h2= page_title
          = render "near_title"
        = render "flashes"
        = yield

      = render "footer"
