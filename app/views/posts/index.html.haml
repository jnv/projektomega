%h1 Diskuze

-if can? :create, :posts
  = link_to 'Nový příspěvek', new_post_path

%section#posts
  -@posts.each do |post|
    %article[post]
      %p
        %strong= link_to post.character.full_name, post.character
      %p
        = post.content
      -if can? :edit, post
        %p
          = link_to 'Upravit', edit_post_path(post)

-if can? :create, :posts
  = link_to 'Nový příspěvek', new_post_path

