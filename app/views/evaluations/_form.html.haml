%p
  Hodnocení agenta
  %strong= resource.character
  v misi
  %strong= resource.mission

= simple_form_for resource do |f|
  .form-inputs
    = f.association :author,  disabled: disable_flag if can? :create, resource, :author
    = f.input :mission_id, as: :hidden
    = f.input :character_id, as: :hidden
    = f.input :content
  .form-actions
    =f.button :submit
